[project]
name = "webhook-service"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "orjson>=3.11.9",
    "structlog>=26.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.2",
]
